# HomeWaterCounters

Гибридное приложение для передачи показаний счётчиков воды и электричества.
Две части, связанные **только через папку Dropbox** — без своего сервера, без FCM,
без публичных эндпоинтов.

* **Мобильное (.NET MAUI, Android)** — съёмка счётчиков, загрузка фото, напоминания,
  подтверждение распознанных значений, единственная точка настройки обеих частей.
* **Десктопное (Windows, .NET 8)** — распознавание фото локальной VLM-моделью на GPU,
  прогноз при отсутствии фото, автоматический ввод в личный кабинет через Playwright,
  письмо на почту.

Участие человека сведено к двум действиям: сфотографировать счётчики и подтвердить
на телефоне то, что распознала модель.

## Состояние

| Фаза | Что | Статус |
|---|---|---|
| 0 | Ядро: контракты, очередь сообщений, шифрование секретов, прогноз, валидация | ✅ 94 теста |
| 1 | Транспорт Dropbox: PKCE-авторизация, `IRemoteStore`, longpoll | ✅ проверено на боевом Dropbox |
| 2 | Распознавание: схема ответа, Ollama и OpenAI-совместимые, ансамбль, предобработка | ✅ 46 тестов |
| 5 | Адаптер личного кабинета на Playwright | ✅ 13 тестов на живом Chromium |
| 6–7 | Десктопный хост: наблюдатель, обработчик очереди, watchdog, почта | ✅ 47 тестов |
| 2* | Подбор модели замерами по фикстурам | ⏳ нужна машина с GPU и реальные фотографии |
| 3–4 | Мобильное приложение и петля подтверждения | ⛔ нужен workload `maui-android` и JDK 21 |

Обработчик собран и проверен целиком на заглушках: `StubRecognizer` вместо VLM,
`InMemoryRemoteStore` вместо Dropbox, макет кабинета вместо сайта поставщика. Что
осталось сделать на машине с GPU — прогнать `WaterCounters.Recognition.Bench` по
реальным фотографиям и выбрать модель по замерам, а не на глаз.

## Документация

| Документ | О чём |
|---|---|
| [docs/architecture.md](docs/architecture.md) | общий замысел, почему Dropbox используется как очередь |
| [docs/dropbox-protocol.md](docs/dropbox-protocol.md) | раскладка папок, контракт сообщений, жизненный цикл задачи |
| [docs/recognition-service.md](docs/recognition-service.md) | спецификация обработчика фотографий |
| [docs/setup-gpu-host.md](docs/setup-gpu-host.md) | что поставить на машине обработчика |

## Архитектура

```
┌──────────────────────────┐          ┌─────────────────────────────┐
│  Android (MAUI)          │          │  Windows Desktop (WPF+Host) │
│  съёмка фото             │          │  DropboxWatcher (longpoll)  │
│  локальные уведомления   │          │  MessageProcessor           │
│  экран подтверждения     │          │  IMeterRecognizer → VLM/GPU │
│  ВСЕ настройки           │          │  IPortalAdapter (Playwright)│
│  offline-очередь (SQLite)│          │  Forecaster, Mailer         │
└───────────┬──────────────┘          └──────────────┬──────────────┘
            │                                        │
            └──────────────► Dropbox App Folder ◄─────┘
                    /config  /photos  /queue  /state
```

Dropbox используется не как хранилище, а как **очередь сообщений с состоянием**.
Жизненный цикл задачи: `pending → processing → done | failed`. Переход в `processing`
делается атомарным `MoveV2`, поэтому одну задачу не могут забрать два обработчика,
а файл, оставшийся в `processing` после падения процесса, — это ровно то, что нужно
переобработать при следующем запуске.

## Структура

```
src/
  WaterCounters.Core/            контракты, IRemoteStore, Dropbox, крипто, прогноз,
                                 валидация, настройки, история, расчёт срока
  WaterCounters.Portal/          IPortalAdapter, Playwright, карта селекторов
  WaterCounters.Recognition/     IMeterRecognizer, схема ответа, Ollama и OpenAI-фасады,
                                 ансамбль с голосованием, предобработка OpenCV
  WaterCounters.Desktop/         хост: трей, наблюдатель, конвейер, watchdog, почта
tools/
  WaterCounters.DropboxSetup/    CLI авторизации Dropbox и smoke-прогон
  WaterCounters.Recognition.Bench/ замер модель × промпт × препроцессинг по фикстурам
tests/
  WaterCounters.Core.Tests/
  WaterCounters.Portal.Tests/      макет кабинета + прогон настоящим Chromium
  WaterCounters.Recognition.Tests/ заглушечный VLM-хост, GPU не нужен
  WaterCounters.Desktop.Tests/     готовность пачки, конвейер, идемпотентность
```

## Сборка и тесты

```powershell
dotnet build WaterCounters.sln
dotnet test  WaterCounters.sln
```

Браузер для тестов адаптера кабинета скачивается автоматически при первом
`dotnet test` — отдельная команда не нужна. Если хочется получить его заранее
(например, до отключения от сети), можно сделать это вручную после сборки:

```powershell
tests/WaterCounters.Portal.Tests/bin/Debug/net8.0/playwright.ps1 install chromium
```

## Запуск обработчика

```powershell
dotnet run --project src/WaterCounters.Desktop
```

Приложение уходит в трей. При первом запуске оно спрашивает мастер-пароль от
`secrets.enc` (можно пропустить — тогда не будет доступа к кабинету и почте) и
создаёт `/config/settings.json` с тремя типовыми счётчиками и включённым режимом
проверки. Журнал — `%LOCALAPPDATA%\WaterCounters\logs`, локальное состояние —
SQLite там же.

Дальше достаточно положить фотографии в `/photos/<yyyy-MM>/` с именами по ключам
счётчиков (`cold-water.jpg`, `hot-water.jpg`, `electricity.jpg`): обработчик заметит
их сам, распознает и пришлёт письмо со значениями и скриншотом заполненной формы.

Локальные настройки машины (пути, интервалы, видимый браузер) — в
`src/WaterCounters.Desktop/appsettings.json`; всё, что одинаково для всех устройств,
живёт в `/config/settings.json` в Dropbox и правится с телефона.

## Выбор модели распознавания

Не на глаз, а замерами. На вход — папка размеченных фикстур, на выход — таблица по
каждой комбинации модель × промпт × препроцессинг:

```powershell
dotnet run --project tools/WaterCounters.Recognition.Bench -- `
  --fixtures fixtures/meters `
  --models qwen2.5vl:7b,gemma3:12b `
  --prompts Russian,English `
  --preprocess on,off `
  --csv bench.csv
```

Фикстура — файл вида `fixtures/meters/cold-water_01234.567_12-345-678.jpg`: ключ
счётчика, ожидаемое значение, серийный номер. Разрядность берётся из самой разметки,
поэтому фикстура самодостаточна и настроек не требует. Утилита возвращает ненулевой
код, пока доля точных совпадений по целой части не дотягивает до 95 %.

Каждая неверно распознанная фотография добавляется в фикстуры и становится
регрессионным тестом. Сами фотографии в репозиторий не попадают — они содержат
серийные номера и привязаны к конкретному адресу.

## Настройка Dropbox

App key в репозитории не хранится. Перед первой сборкой укажите свой — одним из способов:

```powershell
# вариант 1: локальный файл (в .gitignore)
Copy-Item dropbox.local.props.example dropbox.local.props
# и подставить ключ внутри

# вариант 2: переменная окружения
$env:WATERCOUNTERS_DROPBOX_APP_KEY = 'ваш-ключ'
```

Ключ подставляется в константу на этапе сборки: на Android переменных окружения нет,
и передать его в рантайме иначе нельзя. Без настроенного ключа сборка проходит,
а при первом обращении возникает внятная ошибка.

Дальше:

```powershell
dotnet run --project tools/WaterCounters.DropboxSetup -- login
dotnet run --project tools/WaterCounters.DropboxSetup -- smoke
```

`login` проводит авторизацию по OAuth 2.0 Authorization Code + PKCE и кладёт
refresh-токен в `%LOCALAPPDATA%\WaterCounters\dropbox-token.dat` под DPAPI —
файл читается только той же учётной записью Windows на той же машине.

`smoke` прогоняет боевые операции в папке приложения и проверяет свойства, на которых
держится очередь: конфликт при повторной записи вместо автопереименования, атомарность
`MoveV2`, реакцию longpoll и полный цикл сообщения с дедупликацией.

В консоли приложения Dropbox должны быть указаны redirect URI `http://localhost:53682/oauth`
(для десктопа) и `wc-app://auth` (для мобильного), а также разрешения
`files.metadata.read`, `files.content.read`, `files.content.write`.

## Требования

* .NET SDK 8.0.4xx (версия зафиксирована в `global.json`)
* Для мобильной части: workload `maui-android` и **JDK 21**
  (пакет net8 принимает 11.0…21.0.99, поэтому OpenJDK 25 будет отвергнут)
* Для распознавания: NVIDIA GPU с compute capability ≥ 5.0 и от 8 ГБ VRAM,
  плюс Ollama или любой OpenAI-совместимый VLM-сервер

## Заметки о безопасности

* **App key Dropbox** в репозитории не хранится: значение приходит из
  `dropbox.local.props` или переменной окружения и подставляется в константу на этапе
  сборки. При потоке PKCE ключ не является секретом в криптографическом смысле — он
  неизбежно попадает в собранное приложение, а защиту даёт code_verifier, — но он
  идентифицирует конкретное приложение, поэтому публиковать его незачем.
  App secret не используется вовсе.
* **Секреты кабинета и SMTP** шифруются AES-256-GCM с ключом из PBKDF2-SHA256
  (600 000 итераций) перед записью в Dropbox; заголовок входит в associated data,
  поэтому подмена числа итераций ломает проверку так же, как подмена шифротекста.
* **Фотографии счётчиков** в репозиторий не попадают: они содержат серийные номера
  и привязаны к конкретному адресу.
